# ☕ Kape Ko — Track A Level 2
### SONG Commerce AI Labs · Season 1 · Stack: Node.js + React TypeScript

## What is in this repo
Level 2 exercise. catalogService.js and contentService.js have TODO prompts — write them yourself.

## Labs covered — Labs 3, 4, 5

## Quick Start
```bash
# Terminal 1 — Backend
cd backend
npm install && npm run dev

# Terminal 2 — Frontend
cd frontend && npm install && npm run dev -- --host
```

## API Endpoint reference
- POST /api/catalog/generate  → Lab 3
- GET  /api/products          → Lab 5
- POST /api/chat              → Lab 6
- GET  /api/search?q=...      → Lab 7
- GET  /api/products/:id/recommendations → Lab 8
- POST /api/agent/order       → Lab 9
- POST /api/agent/catalog/pipeline → Lab 10
